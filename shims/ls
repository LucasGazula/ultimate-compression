#!/usr/bin/env bash
SHIM_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
UC_DIR="$(dirname "$SHIM_DIR")"
REAL_LS=$(PATH=$(echo "$PATH" | sed -E "s|$SHIM_DIR:?||g") which ls)
"$REAL_LS" "$@" | python3 "$UC_DIR/uc" rtk-filter "ls"
