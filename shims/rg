#!/usr/bin/env bash
SHIM_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
UC_DIR="$(dirname "$SHIM_DIR")"
REAL_RG=$(PATH=$(echo "$PATH" | sed -E "s|$SHIM_DIR:?||g") which rg)
if [ -x "$REAL_RG" ]; then
  "$REAL_RG" "$@" | python3 "$UC_DIR/uc" rtk-filter "rg"
else
  # Pass-through failure if not installed
  exit 127
fi
